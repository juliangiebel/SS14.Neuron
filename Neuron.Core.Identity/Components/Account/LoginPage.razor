@using Microsoft.AspNetCore.Antiforgery
@using Microsoft.AspNetCore.Authentication
@using Neuron.Common.Components.Extensions

@inherits PageBase
@layout PageLayout

<h1>Login</h1>
<form method="post">
    <input name="@Token.FormFieldName" type="hidden" value="@Token.RequestToken">
    <label for="email">Email</label>
    <input id="email" name="email" required type="text" autocomplete="username">
    <label for="password">Password</label>
    <input id="password" name="password" required type="password" autocomplete="current-password">
    <label for="remember-me">
        <input id="remember-me" name="rememberme" type="checkbox" value="true">
        Remember me
    </label>
    <button type="submit">Log in</button>
    <ul>
       @foreach (var error in Context?.GetErrors() ?? [])
       {
           <li>@error</li>
       }
    </ul>
</form>

@code
{
    [Parameter]
    public string ReturnUrl { get; set; } = null!;
    
    [Parameter]
    public List<AuthenticationScheme> ExternalLogins { get; set; } = null!;

    [Parameter]
    public AntiforgeryTokenSet Token { get; set; } = null!;

}
