using Microsoft.AspNetCore.Builder;

namespace Neuron.OpenId.Endpoints;

public static class Endpoints
{
    public static void MapNeuronOpenIdEndpoints(this WebApplication app)
    {
        
    }
}